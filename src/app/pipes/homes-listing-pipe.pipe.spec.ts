import { HomesListingPipePipe } from './homes-listing-pipe.pipe';

describe('HomesListingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HomesListingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
